read_from_file  определение (имя файла):
    открыть = файл (имя файла, 'r')
    кортеж = имена(file.readline()[:-1].split(','))
    = данные []
    Встроенная панель инструментов действий с файлами Копировать строки   Копировать строки    для:
          Копировать постоянную ссылку   = Просмотр git blame.Ссылка в новом выпуске(',')
        данные.Перейти к строке((Перейти к строке…(строка [0]),  Вперед (строка [1]), строка[2], int(строка [3]), int(строка [4]), строка [5][:-1]))
    " = names_str.join([str(каждый).всего (15) для  каждого  в  именах])
    печать(names_str)
    данные  в  tup  для:
        " = tup_str.join([str(каждый).всего (15) для  каждого  в  tup])
        печать (tup_str)
    имена  возвращаются, данные


write_to_file  определение (имена, данные):
    открыть = файл ('d2.csv', 'w')
    файл.написать(','.присоединиться (имена)+'\n')
    данные  в  строке  для:
        = строка [str(i) для  i  в  строке]
        файл.написать(','.присоединиться (строка)+'\n')
    возвращаемый  файл


определение  новых данных(имя_файла):
    = new_data []
    открыть = файл(имя_файла, 'a')
    i = 0
    5 <= я  в то время как:
        ввод = создать()
        new_data.добавить(новый)
        i += 1
    файл.написать(','.join(new_data) + '\n')


имена, данные = read_from_file('data.csv')
write_to_file(имена, данные)
print("Ввести данные?")
печать ("1) Да")
печать ("2) Нет")
ввод = ответ()
'1' == ответить , если:
    new_data('data.csv')
ещё:
    выход()
